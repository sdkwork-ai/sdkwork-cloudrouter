apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sdkwork-cloudrouter-edge
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-Frame-Options "DENY" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - router.sdkwork.com
        - router.birdcoder.com
        - router.dtupay.com
        - cloudrouter.sdkwork.com
      secretName: sdkwork-cloudrouter-edge-tls
  rules:
    - host: router.sdkwork.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sdkwork-cloudrouter-edge
                port:
                  number: 3900
    - host: router.birdcoder.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sdkwork-cloudrouter-edge
                port:
                  number: 3900
    - host: router.dtupay.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sdkwork-cloudrouter-edge
                port:
                  number: 3900
    - host: cloudrouter.sdkwork.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sdkwork-cloudrouter-edge
                port:
                  number: 3900
