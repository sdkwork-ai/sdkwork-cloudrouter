# @sdkwork/clawrouter-pc-admin-messaging

Admin control-plane surface for external SMS/email messaging delivery configuration.

Integrates with `@sdkwork/clawrouter-backend-sdk` through `messagingService.ts`; do not call messaging backend routes with raw HTTP.
