/** Service provider dashboard response schema exposed by Claw Router. */
export interface ServiceProviderDashboardResponse {
  /** Item field on service provider dashboard response. */
  item: Record<string, unknown>;
}
