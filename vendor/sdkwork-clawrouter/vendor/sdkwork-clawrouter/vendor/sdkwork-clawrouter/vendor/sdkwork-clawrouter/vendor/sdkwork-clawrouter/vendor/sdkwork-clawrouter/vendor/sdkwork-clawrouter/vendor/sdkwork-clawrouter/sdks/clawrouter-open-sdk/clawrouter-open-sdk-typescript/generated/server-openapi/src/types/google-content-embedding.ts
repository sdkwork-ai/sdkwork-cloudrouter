import type { ProviderJsonValue } from './provider-json-value';

/** Google Gemini google content embedding schema exposed by Claw Router vendor routing. */
export interface GoogleContentEmbedding {
  /** Embedding vector values. */
  values?: number[];
}
