import type { AdminChannelGroupItem } from './admin-channel-group-item';

/** Admin channel group mutation response schema exposed by Claw Router. */
export interface AdminChannelGroupMutationResponse {
  /** Item field on admin channel group mutation response. */
  item: AdminChannelGroupItem;
}
