[workspace]

[package]
name = "clawrouter-open-sdk"
version = "0.1.0"
edition = "2021"
description = "SDKWork Claw Router OpenAI-compatible gateway SDK rust generated transport SDK"
license = "MIT"
authors = ["SDKWork Team"]

[lib]
name = "clawrouter_open_sdk"
path = "src/lib.rs"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "rustls-tls"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
http = "1.0"

[dev-dependencies]
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
