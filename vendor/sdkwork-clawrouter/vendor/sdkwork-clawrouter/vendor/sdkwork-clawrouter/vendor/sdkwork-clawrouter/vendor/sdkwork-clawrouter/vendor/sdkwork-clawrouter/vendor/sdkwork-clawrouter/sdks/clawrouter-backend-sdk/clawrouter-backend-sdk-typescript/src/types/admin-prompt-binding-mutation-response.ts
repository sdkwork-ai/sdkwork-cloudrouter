import type { AdminPromptBindingItem } from './admin-prompt-binding-item';

/** Admin prompt binding mutation response schema exposed by Claw Router. */
export interface AdminPromptBindingMutationResponse {
  /** Item field on admin prompt binding mutation response. */
  item: AdminPromptBindingItem;
}
