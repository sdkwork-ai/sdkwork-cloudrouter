import type { AdminModelVendorItem } from './admin-model-vendor-item';

/** Admin model vendor mutation response schema exposed by Claw Router. */
export interface AdminModelVendorMutationResponse {
  /** Item field on admin model vendor mutation response. */
  item: AdminModelVendorItem;
}
