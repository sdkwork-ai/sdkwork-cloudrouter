import type { AdminMcpToolItem } from './admin-mcp-tool-item';

/** Admin mcp tool mutation response schema exposed by Claw Router. */
export interface AdminMcpToolMutationResponse {
  /** Item field on admin mcp tool mutation response. */
  item: AdminMcpToolItem;
}
