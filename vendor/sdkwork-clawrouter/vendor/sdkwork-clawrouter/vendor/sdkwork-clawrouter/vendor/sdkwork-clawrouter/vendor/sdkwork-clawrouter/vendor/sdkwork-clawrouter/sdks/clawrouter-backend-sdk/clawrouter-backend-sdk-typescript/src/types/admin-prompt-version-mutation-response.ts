import type { AdminPromptVersionItem } from './admin-prompt-version-item';

/** Admin prompt version mutation response schema exposed by Claw Router. */
export interface AdminPromptVersionMutationResponse {
  /** Item field on admin prompt version mutation response. */
  item: AdminPromptVersionItem;
}
