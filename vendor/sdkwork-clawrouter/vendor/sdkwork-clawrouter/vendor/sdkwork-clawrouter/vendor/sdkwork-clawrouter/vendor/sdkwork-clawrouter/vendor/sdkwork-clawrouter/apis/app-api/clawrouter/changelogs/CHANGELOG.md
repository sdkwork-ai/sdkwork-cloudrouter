# Claw Router App API Changelog

## 0.3.0

- App API contract materialized to `clawrouter-app-api.openapi.json` with SDKWork request context extensions.
- Runtime readiness probes extended for configured Redis dependencies.

## 0.2.x

- Initial app-api domain grouping under `apis/app-api/clawrouter/`.
