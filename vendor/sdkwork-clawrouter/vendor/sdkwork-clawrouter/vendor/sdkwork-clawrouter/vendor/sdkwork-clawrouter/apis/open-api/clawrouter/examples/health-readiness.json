{
  "description": "Operational health and readiness probes for Claw Router HTTP surfaces.",
  "requests": [
    {
      "name": "healthz",
      "method": "GET",
      "path": "/healthz",
      "expectedStatus": 200
    },
    {
      "name": "readyz",
      "method": "GET",
      "path": "/readyz",
      "expectedStatus": 200,
      "notes": "Returns 503 when database, configured Redis, or enabled usage-settlement schema checks fail."
    }
  ]
}
