/** Admin account model mappings replace response schema exposed by Claw Router. */
export interface AdminAccountModelMappingsReplaceResponse {
  /** Ok field on admin account model mappings replace response. */
  ok: boolean;
}
