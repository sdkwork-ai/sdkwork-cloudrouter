import type { AdminChannelGroupChannelBindingItem } from './admin-channel-group-channel-binding-item';

/** Admin channel group channel bindings response schema exposed by Claw Router. */
export interface AdminChannelGroupChannelBindingsResponse {
  /** Items field on admin channel group channel bindings response. */
  items: AdminChannelGroupChannelBindingItem[];
}
