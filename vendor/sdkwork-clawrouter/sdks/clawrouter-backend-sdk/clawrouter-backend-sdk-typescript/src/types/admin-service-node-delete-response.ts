/** Admin service node delete response schema exposed by Claw Router. */
export interface AdminServiceNodeDeleteResponse {
  /** Deleted field on admin service node delete response. */
  deleted: boolean;
}
