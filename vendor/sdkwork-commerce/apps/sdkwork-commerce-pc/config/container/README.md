# Container Config

Container runtime config templates for packaged Commerce PC/server profiles live here when this root enables container delivery.

Allowed content: safe profile templates and mounted-secret placeholders. Forbidden content: image-baked secrets, tokens, mutable runtime data, database files, or live production values.
