# Tauri Config

Tauri platform config templates and notes live here when desktop or tablet targets are enabled.

Allowed content: bundle identifiers, package ids, icons, permissions, capabilities, window metadata, and signing references with placeholders. Forbidden content: business API contracts, SDK ownership metadata, auth tokens, private keys, or signing secrets.
