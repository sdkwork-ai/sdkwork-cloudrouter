# Desktop Config

Desktop user runtime config templates for installed or local desktop modes live here.

Allowed content: safe example desktop profile templates and local service toggles. Forbidden content: signing secrets, auth tokens, refresh tokens, production credentials, private keys, or committed local overrides.
