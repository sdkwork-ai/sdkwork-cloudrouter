# App Tests

Application-level architecture, package-boundary, config, route, and runtime verification for `sdkwork-commerce-pc` lives here.

Package-local tests stay inside their owning package. Shared test fixtures may live under `tests/test-utils/` when they are used across app packages.
