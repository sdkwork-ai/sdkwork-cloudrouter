# App SDK Workspace

Application-root SDK integration notes and app-local SDK workspace metadata live here.

Repository-owned Commerce SDK families currently live at the repository root under `sdks/`. If this app root starts owning generated SDK families directly, follow `../../../sdkwork-specs/SDK_SPEC.md` and `../../../sdkwork-specs/SDK_WORKSPACE_GENERATION_SPEC.md` before adding generated outputs.
