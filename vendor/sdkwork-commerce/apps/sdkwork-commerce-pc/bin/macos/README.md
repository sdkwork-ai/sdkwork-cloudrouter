# macos

Purpose: macOS-specific operational scripts for the SDKWork Commerce PC application root.

Allowed content: shell wrappers for macOS diagnostics, desktop launch helpers, notarization handoff checks, and packaging helpers.

Forbidden content: signing private keys, private credentials, generated SDK output, machine-specific absolute paths, and business workflow implementation.

Verification: run the repository architecture check before adding or changing platform scripts.
