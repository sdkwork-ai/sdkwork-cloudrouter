# migrations/postgres

Versioned Cloud Router rating and charging migrations. The initial
installation is owned by
`ddl/baseline/postgres/0001_cloudrouter_billing_baseline.sql`; future changes
use paired `{version}_{name}.up.sql` and `{version}_{name}.down.sql` files.
