# migrations

Baseline-plus-migrations strategy: schema changes after the baseline ship as
numbered migrations here. The baseline anchor table is
`commerce_payment_statement`.

No migrations yet — the initial baseline is `0001_payment_reconciliation_baseline.sql`.
