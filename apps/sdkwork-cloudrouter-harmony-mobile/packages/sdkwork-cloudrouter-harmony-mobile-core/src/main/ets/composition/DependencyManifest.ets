/**
 * Composition manifest pointer.
 *
 * Authority: `APP_COMPOSITION_SPEC.md`. Value is owned by the repository
 * composition aligner; consumers must not re-derive it.
 */
export const sdkworkComponentSpecPath: string = '../../../specs/component.spec.json';
