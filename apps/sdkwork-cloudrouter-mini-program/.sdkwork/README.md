# SDKWork Workspace

This `.sdkwork/` directory is source-controlled workspace metadata for
`sdkwork-cloudrouter-mini-program`. It holds local agent extensions only; SDKWork
rules come from `../sdkwork-specs`.
