# @sdkwork/cloudrouter-mp-console-api-keys

API key listing, creation, and revocation for the mini-program root.

The page module only reads through `CloudRouterConsolePorts` handed in by the root;
it never imports the generated SDK.
