# @sdkwork/cloudrouter-service

Architecture-neutral Cloud Router console services: payload normalization,
read-model loading through `CloudRouterConsolePorts`, and locale-aware
formatting. Single-call entrypoints combine the multi-step SDK setup each
client root would otherwise repeat.
