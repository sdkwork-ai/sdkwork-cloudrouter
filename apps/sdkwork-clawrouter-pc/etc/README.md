# PC Source Configuration

`browser.runtime.json` owns the Claw Router PC renderer bind and runtime binding names. Public
application origins and API Base URLs come from the repository deployment index and are injected by
bootstrap. Secrets and local overrides are not committed here.
