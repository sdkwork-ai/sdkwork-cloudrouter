# @sdkwork/cloudrouter-h5-console-catalog

Official model and pricing catalog for the H5 root.

Aligned with the PC surface paths `/models`, `/pricing`, `/rankings`.
Loads data through `@sdkwork/cloudrouter-service` read models over
`CloudRouterConsolePorts` injected by the app root; it never imports a generated SDK.
