# src/shell/

H5 shell registration. The shell implementation lives in
`packages/sdkwork-cloudrouter-h5-shell`; this directory holds only the app-level
registration and re-export used by `src/App.tsx`.
