# config/

H5 host and browser configuration. Browser runtime-env profile sources live under
`../etc/browser/` because this repository keeps the deployment index in `etc/`
(`SOURCE_CONFIG_SPEC.md`); this directory holds host-specific additions.

- `host/`: browser and (when the Capacitor profile ships) capacitor host configuration.
