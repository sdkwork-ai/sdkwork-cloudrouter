# config/host/

Host adapter configuration for the H5 root. Values here must contain no secrets;
credential-bearing bootstrap files belong in `etc/` as `*.local.json` variants that
stay out of version control.
