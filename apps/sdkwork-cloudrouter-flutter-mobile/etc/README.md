# etc/

Deployment index for the Flutter mobile root.

`sdkwork.deployment.config.json` delegates to the repository deployment index and
materializes the Dart define payload consumed by `String.fromEnvironment`.
