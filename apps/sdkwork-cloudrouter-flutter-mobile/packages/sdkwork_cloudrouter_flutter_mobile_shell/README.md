# sdkwork_cloudrouter_flutter_mobile_shell

Navigation registry and auth gate decision for the CloudRouter Flutter mobile
root.

Route ids follow `<surface>.<domain>.<capability>.<screen>` and are reused
unchanged from `CLOUDROUTER_CONSOLE_ROUTES` so PC, H5, mini program, Flutter, and
HarmonyOS roots stay comparable.
