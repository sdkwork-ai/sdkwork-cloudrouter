# Superseded Schema Registry Design

Status: superseded
Owner: SDKWork maintainers
Updated: 2026-07-31

This stable path is a redirect. The migrated document was corrupted and mixed
historical page mappings with retired database and route ownership. It is not a
schema, API, SDK, or acceptance authority.

Use the [technical architecture](TECH_ARCHITECTURE.md), the
[schema registry catalog](../../schema-registry/table-catalog.md), authored
contracts under `docs/schema-registry/`, repository `specs/`, and generated
schema artifacts that identify their source contract.

The retired `/admin/channel` path and historical provider/site/channel tables
must not be restored from this file.
