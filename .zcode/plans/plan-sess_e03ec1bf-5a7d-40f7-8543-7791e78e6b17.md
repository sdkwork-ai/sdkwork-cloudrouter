# 应用重命名：sdkwork-cloudrouter → sdkwork-cloudrouter（全 workspace 范围）

## 已确认决策
1. 所有 `sdkwork-cloudrouter-*` 短前缀（共享基建 crate/服务，如 sdkwork-cloudrouter-config、sdkwork-cloudrouter-http、sdkwork-cloudrouter-installer）→ `sdkwork-cloudrouter-*`（sdkwork-cloudrouter-config、sdkwork-cloudrouter-http…）
2. `cloudhub` 品牌 → `cloudhub`（data/skills/cloudhub → cloudhub、mirror-cloudhub-skills-seed.mjs → mirror-cloudhub-skills-seed.mjs 等）
3. 数据库一并重命名：`0001_cloudrouter_baseline.sql` → `0001_cloudrouter_baseline.sql`（含内部 schema/注释）；兄弟仓库 DDL 中 `cloud_router_*` 列 → `cloud_router_*`
4. 兄弟仓库最大范围：真实依赖 + sdkwork-specs + 工作区根 + 全部仓库 AGENTS.md 样板 + 全部 docs/README/营销文档提及

## 统一命名映射（大小写敏感，先长后短，全局 token 替换）
- `CloudRouter` → `CloudRouter`；`cloudRouter` → `cloudRouter`；`CLOUDROUTER` → `CLOUDROUTER`；`cloudrouter` → `cloudrouter`（覆盖 cloudrouter_、cloudrouterctl、sdkwork-cloudrouter、routes/apis 全链）
- `cloud_router` → `cloud_router`（含 `SDKWORK_CLOUDROUTER_*`、`cloud_router_*` DB 列）
- `cloud_` → `cloud_`（SDKWORK_CLOUDROUTER_REDIS_* 等）；`cloud-` → `cloud-`（cloud-router、sdkwork-cloudrouter-、cloud-provider-adapter 等）
- `cloudhub` → `cloudhub`；`cloudroutes` → `cloudroutes`（@sdkwork/cloudroutes-pc-commons 平行改名）
- 词边界 `cloud` → `cloud`（显示名 "Cloud Router"、desktopAppId 中 cloud.router 等）
- 目录/文件名重命名遵循同名映射（git mv 保留跟踪）
- **明确排除**：external/（openclaw、hermes-agent、cc-switch 第三方子模块）、node_modules、target、dist、.git、.pnpm-store、.ruff_cache、.tmp、tmp、target-test-fixtures（测试垃圾）、.zcode/（本地会话文件）、sdkwork-xiangqi 的 Clouds 游戏功能（不同产品）
- pnpm-lock.yaml / Cargo.lock 不手工改，重命名后重新生成

## 阶段 1：当前仓库内容改造（E:\sdkwork-space\sdkwork-cloudrouter）
1. 在仓库外临时位置（E:\sdkwork-space\.tools\）编写一次性 Node 迁移脚本：遍历文本文件（扩展名白名单 + 无 NUL），按上述 token 表替换；目录与文件级 git mv 重命名清单
2. 目录重命名（git mv，约 120+ 处）：
   - `apps/sdkwork-cloudrouter-common`、`apps/sdkwork-cloudrouter-pc` + 内部 40 个 `sdkwork-cloudrouter-pc-*` packages + `sdkwork-cloudroutes-pc-commons`
   - crates：`sdkwork-cloudrouter-*`（14）与 `sdkwork-cloudrouter-*`（10）→ `sdkwork-cloudrouter-*`；`sdkwork-routes-cloudrouter-*`（6）→ `sdkwork-routes-cloudrouter-*`；`sdkwork-api-cloudrouter-*`（2）→ `sdkwork-api-cloudrouter-*`（Cargo.toml 中 crate name 与 lib name 同步，snake_case 由 token 覆盖）
   - services 5 个；sdks/cloudrouter-{app,backend,open}-sdk + 内部 9 语言目录；dks/cloudrouter-open-sdk；scripts/cloud-router-*.mjs（~40）；tools/cloudrouter_*.py（~10）+ generate-cloudrouter-*.mjs；tests/test_cloudrouter_*.py（~15）；deployments/kubernetes、prometheus、grafana、systemd；cloud-router-audit/；etc/cloudrouter.database.example.toml；database/0001_cloudrouter_baseline.sql；docs/schema-registry/sdkwork-cloudrouter.tables.yaml；generated/schema/registry/…tables.effective.yaml；generated/openapi/cloudrouter-*.json；generated/types/java/com/sdkwork/cloud/；docs 中带 cloud 文件名的 PRD/TECH 文档；apis/*/cloudrouter/ 目录与文件；.agents/skills/cloudrouter-*；data/skills/cloudhub/
3. 身份文件精改：sdkwork.app.config.json（key/displayName/desktopAppId com.sdkwork.sdkwork.cloud.router.desktop/containerImage/CDN URL/权限 scope/install 命令/release 文案）、sdkwork.workflow.json（app.id/name/repository Sdkwork-Cloud/sdkwork-cloudrouter/artifactPrefix/env/脚本）、apis/manifest.json（application/domain/apiAuthority/sdkFamily，并重新计算三个 openapi 文件的 sha256）、package.json（name= sdkwork-cloudrouter-workspace、脚本、--product-prefix cloudrouter）、pnpm-workspace.yaml、Cargo.toml、.github/*（verify.yml、模板、CODEOWNERS、dependabot）、.env.* 与 etc/ 部署配置（环境变量统一为 SDKWORK_CLOUDROUTER_* / SDKWORK_CLOUDROUTER_*）、.cargo/config.toml、specs/naming-migration.manifest.json（canonicalApplicationCode=cloudrouter、legacyRepositoryStems 记录 sdkwork-cloudrouter）、.sdkwork/、.agents/openai.yaml
4. 生成物与 SDK：generated/、sdks/、dks/ 生成代码 in-place 替换（C# 命名空间 Sdkwork.CloudRouter.*、Java/Kotlin 包 com.sdkwork.cloudrouter、Python 模块 sdkwork_cloudrouter_*_sdk、Go/Swift/Flutter/Rust 标识符）；同步更新生成器配置与模板（tools/*.py、bin/generate-sdk.mjs、sdkgen manifests、openapi specs 内容），保证后续重新生成仍产出 cloudrouter 命名
5. 锁文件重建：pnpm install（更新 pnpm-lock.yaml）、cargo metadata / 代表 crate check（更新 Cargo.lock）
6. 最后执行根目录改名：`mv E:\sdkwork-space\sdkwork-cloudrouter E:\sdkwork-space\sdkwork-cloudrouter`（若被进程占用则提示用户关闭占用后手动执行）；随后更新本仓库 .git/config remote URL → Sdkwork-Cloud/sdkwork-cloudrouter.git（GitHub 侧仓库重命名需用户操作，推送前完成）

## 阶段 2：兄弟仓库依赖改造（最大范围）
1. 真实构建/运行依赖（约 25 仓库）：api-cloud-gateway、aiot、image、knowledgebase、models、music、video、voice、appstore、im、mall、iam、payment、order、membership、documents、appbase、assets、audio、utils、database、terminal、prompts、app-topology、web-framework、generations、sdk-generator、rtc、birdcoder（真实引用处）、kernel —— Cargo.toml path 依赖（cloudrouter-open-sdk → cloudrouter-open-sdk、sdkwork-cloudrouter-http → sdkwork-cloudrouter-http、sdkwork-api-cloudrouter-assembly / sdkwork-cloudrouter-database-host 对应改名）、package.json 依赖（@sdkwork/cloudrouter-* → @sdkwork/cloudrouter-*）、pnpm-workspace.yaml 路径（../sdkwork-cloudrouter/... → ../sdkwork-cloudrouter/... 且内部 sdks 路径同步）、vitest.config.ts、CI（sdkwork_cloudrouter_ref / SDKWORK_CLOUDROUTER_REF 等）、源码与文件名（cloud_router.rs → cloud_router.rs 等）、specs、测试文件名（music-cloudrouter-*.test.mjs、no-cloudrouter-runtime-dependency.test.mjs 等）、DB DDL 列名、.env 示例
2. sdkwork-specs（48 文件）：ENVIRONMENT_SPEC/DEPLOYMENT_SPEC/RUNTIME_DIRECTORY_SPEC 等规范正文、tools/ 校验器硬编码（app-sdk-consumer-import-patterns.mjs、route-crate-naming.mjs 等）、workspace/consumers/sdkwork-cloudrouter.json（文件名+内容）
3. 工作区根 E:\sdkwork-space：.gitmodules（submodule path sdkwork-cloudrouter + URL）、AGENTS.md、dependency.md、docs/company 与 docs/resume 等营销文档、.sdkwork 缓存（build-cache-paths.txt 等，或标记为可再生）
4. 最大范围补充：~70 仓库 AGENTS.md 的 "Federated Cloud Router domain surfaces" SDK 样板块 + 各仓库 docs/README/历史文档中所有 cloudrouter 提及；sdkwork-github-workflow/examples/sdkwork-cloudrouter 目录与内容；sdkwork-promotion 的 sdkwork.app.config.json 截图 ID
5. 涉及依赖改名的仓库锁文件重生成（Tier A 的 pnpm install / cargo 更新），其余做 grep 审计

## 阶段 3：验证
1. grep/rg 审计：本仓库与兄弟仓库（排除白名单）`cloudrouter|cloud-|cloud_|cloudRouter|CloudRouter|CLOUDROUTER|cloudhub|cloud\.` 残留应为 0（仅 external/ 第三方与明确排除项）
2. 本仓库：cargo metadata --no-deps 全量解析 + 代表 crate cargo check（router-service / standalone-gateway / edge-runtime）；pnpm install 成功 + 窄范围脚本/单测
3. 兄弟仓库：Tier A 仓库 cargo metadata / pnpm install --offline 路径解析抽查（appstore、im、aiot、models）
4. apis/manifest.json sha256 重新计算并核对 materialize 脚本可运行
5. 输出最终报告：改动统计、验证结果、遗留人工事项清单

## 遗留人工事项（报告中明确列出，不做自动执行）
- GitHub 仓库重命名 Sdkwork-Cloud/sdkwork-cloudrouter → sdkwork-cloudrouter（本地 remote 已更新，推送前完成）
- GitHub CODEOWNERS 团队 @sdkwork/cloudrouter-* → cloudrouter-*（组织侧）
- CDN 资源路径（cdn.sdkwork.com/apps/sdkwork-cloudrouter/...）与已部署环境变量切换需与发布流程同步
- 父仓库（E:\sdkwork-space）gitlink/.gitmodules 变更的提交

## 执行说明
- 不提交任何 git commit（保留工作区改动，用户审查）；git mv 会在本仓库 stage 改名
- 全程按 AGENTS.md 验证路由选择最窄验证；环境变量/DB/部署类变更为用户已确认授权
