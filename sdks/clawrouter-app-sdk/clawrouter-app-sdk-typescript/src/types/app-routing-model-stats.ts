/** App routing model stats schema exposed by Claw Router. */
export interface AppRoutingModelStats {
  /** Lat field on app routing model stats. */
  lat: string;
  /** M field on app routing model stats. */
  m: string;
  /** Req field on app routing model stats. */
  req: string;
  /** Sr field on app routing model stats. */
  sr: string;
  /** Tok field on app routing model stats. */
  tok: string;
}
