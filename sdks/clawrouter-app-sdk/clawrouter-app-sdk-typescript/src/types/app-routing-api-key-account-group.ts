/** App routing api key account group schema exposed by Claw Router. */
export interface AppRoutingApiKeyAccountGroup {
  /** Code field on app routing api key account group. */
  code: string;
  /** Id field on app routing api key account group. */
  id: string;
  /** Name field on app routing api key account group. */
  name: string;
}
