/** Mark notification popup seen response schema exposed by Claw Router. */
export interface MarkNotificationPopupSeenResponse {
  /** State field on mark notification popup seen response. */
  state: 'popup_seen';
  /** Updated field on mark notification popup seen response. */
  updated: boolean;
}
