import type { PageInfo } from './page-info';

/** IpLimitRulePage contract. */
export interface IpLimitRulePage {
  /** items field on IpLimitRulePage. */
  items: Record<string, unknown>[];
  /** pageInfo field on IpLimitRulePage. */
  pageInfo: PageInfo;
}
