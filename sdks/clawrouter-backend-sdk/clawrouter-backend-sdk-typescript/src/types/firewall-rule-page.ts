import type { PageInfo } from './page-info';

/** FirewallRulePage contract. */
export interface FirewallRulePage {
  /** items field on FirewallRulePage. */
  items: Record<string, unknown>[];
  /** pageInfo field on FirewallRulePage. */
  pageInfo: PageInfo;
}
