/** Admin storage default bucket update request schema exposed by Claw Router. */
export interface AdminStorageDefaultBucketUpdateRequest {
  /** Bucket id field on admin storage default bucket update request. */
  bucketId: string;
  /** Reason field on admin storage default bucket update request. */
  reason: string;
}
