/** Admin capacity pair schema exposed by Claw Router. */
export interface AdminCapacityPair {
  /** Total field on admin capacity pair. */
  total: number;
  /** Used field on admin capacity pair. */
  used: number;
}
