/** Admin runtime region settings update request schema exposed by Claw Router. */
export interface AdminRuntimeRegionSettingsUpdateRequest {
  /** Current region code field on admin runtime region settings update request. */
  currentRegionCode?: string;
  /** Current region name field on admin runtime region settings update request. */
  currentRegionName?: string;
  /** Remark field on admin runtime region settings update request. */
  remark?: string;
}
