/** Admin runtime region settings response schema exposed by Claw Router. */
export interface AdminRuntimeRegionSettingsResponse {
  /** Current region code field on admin runtime region settings response. */
  currentRegionCode: string;
  /** Current region name field on admin runtime region settings response. */
  currentRegionName: string;
  /** Remark field on admin runtime region settings response. */
  remark: string;
}
