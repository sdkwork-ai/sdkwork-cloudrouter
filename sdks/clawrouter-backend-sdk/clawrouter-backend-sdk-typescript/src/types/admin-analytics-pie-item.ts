/** Admin analytics pie item schema exposed by Claw Router. */
export interface AdminAnalyticsPieItem {
  /** Color field on admin analytics pie item. */
  color: string;
  /** Name field on admin analytics pie item. */
  name: string;
  /** Value field on admin analytics pie item. */
  value: string;
}
