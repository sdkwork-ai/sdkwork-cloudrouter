import type { UpstreamSupplierEndpointCollection } from './upstream-supplier-endpoint-collection';

/** Upstream supplier endpoint collection response schema exposed by Claw Router. */
export interface UpstreamSupplierEndpointCollectionResponse {
  /** Item field on upstream supplier endpoint collection response. */
  item: UpstreamSupplierEndpointCollection;
}
