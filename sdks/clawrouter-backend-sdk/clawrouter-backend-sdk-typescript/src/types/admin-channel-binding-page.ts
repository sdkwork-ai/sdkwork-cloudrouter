import type { PageInfo } from './page-info';

/** AdminChannelBindingPage contract. */
export interface AdminChannelBindingPage {
  /** items field on AdminChannelBindingPage. */
  items: Record<string, unknown>[];
  /** pageInfo field on AdminChannelBindingPage. */
  pageInfo: PageInfo;
}
