import type { PageInfo } from './page-info';

/** MonitorAlertPage contract. */
export interface MonitorAlertPage {
  /** items field on MonitorAlertPage. */
  items: Record<string, unknown>[];
  /** pageInfo field on MonitorAlertPage. */
  pageInfo: PageInfo;
}
