import type { AdminChannelGroupChannelBindingInput } from './admin-channel-group-channel-binding-input';

/** Admin channel group channel bindings replace request schema exposed by Claw Router. */
export interface AdminChannelGroupChannelBindingsReplaceRequest {
  /** Items field on admin channel group channel bindings replace request. */
  items: AdminChannelGroupChannelBindingInput[];
}
