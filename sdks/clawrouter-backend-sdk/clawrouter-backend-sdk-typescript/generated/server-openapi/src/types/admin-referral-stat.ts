/** Admin referral stat schema exposed by Claw Router. */
export interface AdminReferralStat {
  /** Bonus awarded field on admin referral stat. */
  bonusAwarded: string;
  /** Id field on admin referral stat. */
  id: string;
  /** Inviter field on admin referral stat. */
  inviter: string;
  /** Link field on admin referral stat. */
  link: string;
  /** Total invited field on admin referral stat. */
  totalInvited: string;
  /** Total revenue field on admin referral stat. */
  totalRevenue: string;
}
