import type { PageInfo } from './page-info';

/** ModelLimitRulePage contract. */
export interface ModelLimitRulePage {
  /** items field on ModelLimitRulePage. */
  items: Record<string, unknown>[];
  /** pageInfo field on ModelLimitRulePage. */
  pageInfo: PageInfo;
}
