/** Admin recharge record schema exposed by Claw Router. */
export interface AdminRechargeRecord {
  /** Amount field on admin recharge record. */
  amount: string;
  /** Id field on admin recharge record. */
  id: string;
  /** Method field on admin recharge record. */
  method: string;
  /** Status field on admin recharge record. */
  status: string;
  /** Time field on admin recharge record. */
  time: string;
  /** Trade no field on admin recharge record. */
  tradeNo: string;
  /** Usd credited field on admin recharge record. */
  usdCredited: string;
  /** User field on admin recharge record. */
  user: string;
  /** User id field on admin recharge record. */
  userId: string;
}
