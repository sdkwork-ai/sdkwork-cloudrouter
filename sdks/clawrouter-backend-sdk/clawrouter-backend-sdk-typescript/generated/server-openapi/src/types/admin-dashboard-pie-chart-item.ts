/** Admin dashboard pie chart item schema exposed by Claw Router. */
export interface AdminDashboardPieChartItem {
  /** Color field on admin dashboard pie chart item. */
  color: string;
  /** Name field on admin dashboard pie chart item. */
  name: string;
  /** Value field on admin dashboard pie chart item. */
  value: number;
}
