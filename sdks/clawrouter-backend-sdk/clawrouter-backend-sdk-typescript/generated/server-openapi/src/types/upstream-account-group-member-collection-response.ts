import type { UpstreamAccountGroupMemberCollection } from './upstream-account-group-member-collection';

/** Upstream account group member collection response schema exposed by Claw Router. */
export interface UpstreamAccountGroupMemberCollectionResponse {
  /** Item field on upstream account group member collection response. */
  item: UpstreamAccountGroupMemberCollection;
}
