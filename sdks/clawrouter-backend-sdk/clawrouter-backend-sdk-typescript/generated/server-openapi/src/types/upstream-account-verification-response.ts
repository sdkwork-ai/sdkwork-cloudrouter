import type { UpstreamAccountVerification } from './upstream-account-verification';

/** Upstream account verification response schema exposed by Claw Router. */
export interface UpstreamAccountVerificationResponse {
  /** Item field on upstream account verification response. */
  item: UpstreamAccountVerification;
}
