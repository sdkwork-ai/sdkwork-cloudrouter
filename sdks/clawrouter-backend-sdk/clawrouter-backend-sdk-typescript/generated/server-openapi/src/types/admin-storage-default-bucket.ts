/** Admin storage default bucket schema exposed by Claw Router. */
export interface AdminStorageDefaultBucket {
  /** Bucket id field on admin storage default bucket. */
  bucketId: string;
  /** Bucket name field on admin storage default bucket. */
  bucketName: string;
  /** Id field on admin storage default bucket. */
  id: string;
  /** Logical scope field on admin storage default bucket. */
  logicalScope: string;
  /** Provider code field on admin storage default bucket. */
  providerCode: string;
  /** Provider id field on admin storage default bucket. */
  providerId: string;
  /** Provider type field on admin storage default bucket. */
  providerType: string;
  /** Reason field on admin storage default bucket. */
  reason: string;
  /** Region field on admin storage default bucket. */
  region: string;
  /** Status field on admin storage default bucket. */
  status: string;
  /** Updated at field on admin storage default bucket. */
  updatedAt: string;
}
