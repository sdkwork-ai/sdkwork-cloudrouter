/** Admin storage reconciliation run schema exposed by Claw Router. */
export interface AdminStorageReconciliationRun {
  /** Bucket id field on admin storage reconciliation run. */
  bucketId: string;
  /** Bucket name field on admin storage reconciliation run. */
  bucketName: string;
  /** Completed at field on admin storage reconciliation run. */
  completedAt: string;
  /** Dry run field on admin storage reconciliation run. */
  dryRun: boolean;
  /** Id field on admin storage reconciliation run. */
  id: string;
  /** Issue count field on admin storage reconciliation run. */
  issueCount: string;
  /** Issues field on admin storage reconciliation run. */
  issues: string;
  /** Provider code field on admin storage reconciliation run. */
  providerCode: string;
  /** Provider id field on admin storage reconciliation run. */
  providerId: string;
  /** Run id field on admin storage reconciliation run. */
  runId: string;
  /** Run type field on admin storage reconciliation run. */
  runType: string;
  /** Scope field on admin storage reconciliation run. */
  scope: string;
  /** Started at field on admin storage reconciliation run. */
  startedAt: string;
  /** Status field on admin storage reconciliation run. */
  status: string;
}
