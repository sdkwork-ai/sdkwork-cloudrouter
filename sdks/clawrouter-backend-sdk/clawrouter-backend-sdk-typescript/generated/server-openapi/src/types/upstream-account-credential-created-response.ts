import type { UpstreamAccountCredentialCreated } from './upstream-account-credential-created';

/** Upstream account credential created response schema exposed by Claw Router. */
export interface UpstreamAccountCredentialCreatedResponse {
  /** Item field on upstream account credential created response. */
  item: UpstreamAccountCredentialCreated;
}
