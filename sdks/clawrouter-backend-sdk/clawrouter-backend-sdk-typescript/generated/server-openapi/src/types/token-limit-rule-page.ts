import type { PageInfo } from './page-info';

/** TokenLimitRulePage contract. */
export interface TokenLimitRulePage {
  /** items field on TokenLimitRulePage. */
  items: Record<string, unknown>[];
  /** pageInfo field on TokenLimitRulePage. */
  pageInfo: PageInfo;
}
