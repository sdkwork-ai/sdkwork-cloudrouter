/** Official pricing group facet schema exposed by Cloud Router. */
export interface OfficialPricingGroupFacet {
  /** Code field on official pricing group facet. */
  code: string;
  /** Count field on official pricing group facet. */
  count: string;
  /** Id field on official pricing group facet. */
  id: string;
}
