/** Official pricing value facet schema exposed by Cloud Router. */
export interface OfficialPricingValueFacet {
  /** Code field on official pricing value facet. */
  code: string;
  /** Count field on official pricing value facet. */
  count: string;
  /** Id field on official pricing value facet. */
  id: string;
}
