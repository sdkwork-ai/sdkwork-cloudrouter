/** App invite policy response schema exposed by Cloud Router. */
export interface AppInvitePolicyResponse {
  /** Login required field on app invite policy response. */
  loginRequired: boolean;
  /** Register required field on app invite policy response. */
  registerRequired: boolean;
}
