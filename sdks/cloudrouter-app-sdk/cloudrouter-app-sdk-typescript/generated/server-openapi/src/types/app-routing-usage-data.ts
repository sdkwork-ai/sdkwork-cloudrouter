/** App routing usage data schema exposed by Cloud Router. */
export interface AppRoutingUsageData {
  /** Latency field on app routing usage data. */
  latency: string;
  /** Requests field on app routing usage data. */
  requests: string;
  /** Time field on app routing usage data. */
  time: string;
}
