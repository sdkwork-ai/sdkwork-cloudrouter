/** Official pricing formula term schema exposed by Cloud Router. */
export interface OfficialPricingFormulaTerm {
  /** Coefficient field on official pricing formula term. */
  coefficient: string;
  /** Dimension code field on official pricing formula term. */
  dimensionCode: string;
  /** Term code field on official pricing formula term. */
  termCode: string;
}
