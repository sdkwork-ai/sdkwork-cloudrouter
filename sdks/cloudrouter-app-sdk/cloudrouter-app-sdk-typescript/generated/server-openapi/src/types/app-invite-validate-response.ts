/** App invite validate response schema exposed by Cloud Router. */
export interface AppInviteValidateResponse {
  /** Message field on app invite validate response. */
  message: string;
  /** Valid field on app invite validate response. */
  valid: boolean;
}
