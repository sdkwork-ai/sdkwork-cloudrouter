/** Official pricing region facet schema exposed by Cloud Router. */
export interface OfficialPricingRegionFacet {
  /** Code field on official pricing region facet. */
  code: string;
  /** Count field on official pricing region facet. */
  count: string;
  /** Id field on official pricing region facet. */
  id: string;
}
