/** Official pricing meter facet schema exposed by Cloud Router. */
export interface OfficialPricingMeterFacet {
  /** Code field on official pricing meter facet. */
  code: string;
  /** Count field on official pricing meter facet. */
  count: string;
  /** Display name field on official pricing meter facet. */
  displayName: string;
  /** Id field on official pricing meter facet. */
  id: string;
  /** Unit code field on official pricing meter facet. */
  unitCode: string;
}
