/** Official pricing region option schema exposed by Cloud Router. */
export interface OfficialPricingRegionOption {
  /** Currency code field on official pricing region option. */
  currencyCode: string;
  /** Is global field on official pricing region option. */
  isGlobal: boolean;
  /** Rate count field on official pricing region option. */
  rateCount: number;
  /** Region code field on official pricing region option. */
  regionCode: string;
}
