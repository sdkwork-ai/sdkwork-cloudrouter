/** Upstream supplier model list entry schema exposed by Cloud Router. */
export interface UpstreamSupplierModelListEntry {
  /** Models field on upstream supplier model list entry. */
  models: string[];
  /** Vendor code field on upstream supplier model list entry. */
  vendorCode: string;
}
