import type { UpstreamAccountCredential } from './upstream-account-credential';

/** Upstream account credential item response schema exposed by Cloud Router. */
export interface UpstreamAccountCredentialItemResponse {
  /** Item field on upstream account credential item response. */
  item: UpstreamAccountCredential;
}
