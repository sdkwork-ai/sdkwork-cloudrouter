import type { UpstreamAccountCredentialSecret } from './upstream-account-credential-secret';

/** Upstream account credential secret item response schema exposed by Cloud Router. */
export interface UpstreamAccountCredentialSecretItemResponse {
  /** Item field on upstream account credential secret item response. */
  item: UpstreamAccountCredentialSecret;
}
