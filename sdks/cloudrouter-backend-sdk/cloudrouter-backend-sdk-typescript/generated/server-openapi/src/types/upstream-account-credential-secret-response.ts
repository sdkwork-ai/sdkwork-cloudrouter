/** Upstream account credential secret response schema exposed by Cloud Router. */
export interface UpstreamAccountCredentialSecretResponse {
  /** Credential id field on upstream account credential secret response. */
  credentialId: string;
  /** Secret field on upstream account credential secret response. */
  secret: string;
}
