/** Admin referral strategy mutation request schema exposed by Cloud Router. */
export interface AdminReferralStrategyMutationRequest {
  /** Description field on admin referral strategy mutation request. */
  description?: string;
  /** Ends at field on admin referral strategy mutation request. */
  endsAt?: string;
  /** Max rewards per inviter field on admin referral strategy mutation request. */
  maxRewardsPerInviter?: number;
  /** Name field on admin referral strategy mutation request. */
  name?: string;
  /** Reward target field on admin referral strategy mutation request. */
  rewardTarget?: string;
  /** Reward type field on admin referral strategy mutation request. */
  rewardType?: string;
  /** Reward value field on admin referral strategy mutation request. */
  rewardValue?: string;
  /** Starts at field on admin referral strategy mutation request. */
  startsAt?: string;
  /** Status field on admin referral strategy mutation request. */
  status?: string;
  /** Trigger event field on admin referral strategy mutation request. */
  triggerEvent?: string;
}
