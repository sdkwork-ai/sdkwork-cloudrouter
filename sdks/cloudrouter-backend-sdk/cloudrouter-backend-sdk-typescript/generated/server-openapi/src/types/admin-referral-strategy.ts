/** Admin referral strategy schema exposed by Cloud Router. */
export interface AdminReferralStrategy {
  /** Description field on admin referral strategy. */
  description: string;
  /** Ends at field on admin referral strategy. */
  endsAt: string;
  /** Id field on admin referral strategy. */
  id: string;
  /** Max rewards per inviter field on admin referral strategy. */
  maxRewardsPerInviter: string;
  /** Name field on admin referral strategy. */
  name: string;
  /** Reward target field on admin referral strategy. */
  rewardTarget: string;
  /** Reward type field on admin referral strategy. */
  rewardType: string;
  /** Reward value field on admin referral strategy. */
  rewardValue: string;
  /** Starts at field on admin referral strategy. */
  startsAt: string;
  /** Status field on admin referral strategy. */
  status: string;
  /** Trigger event field on admin referral strategy. */
  triggerEvent: string;
  /** Updated at field on admin referral strategy. */
  updatedAt: string;
}
