import type { UpstreamAccountGroup } from './upstream-account-group';

/** Upstream account group item response schema exposed by Cloud Router. */
export interface UpstreamAccountGroupItemResponse {
  /** Item field on upstream account group item response. */
  item: UpstreamAccountGroup;
}
