/** Upstream account group model list entry schema exposed by Cloud Router. */
export interface UpstreamAccountGroupModelListEntry {
  /** Models field on upstream account group model list entry. */
  models: string[];
  /** Vendor code field on upstream account group model list entry. */
  vendorCode: string;
}
