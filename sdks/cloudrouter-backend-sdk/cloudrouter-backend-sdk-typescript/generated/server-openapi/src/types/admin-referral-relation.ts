/** Admin referral relation schema exposed by Cloud Router. */
export interface AdminReferralRelation {
  /** Claimed at field on admin referral relation. */
  claimedAt: string;
  /** Id field on admin referral relation. */
  id: string;
  /** Invite code field on admin referral relation. */
  inviteCode: string;
  /** Invitee field on admin referral relation. */
  invitee: string;
  /** Inviter field on admin referral relation. */
  inviter: string;
  /** Reward status field on admin referral relation. */
  rewardStatus: string;
  /** Source field on admin referral relation. */
  source: string;
}
