/** Official pricing product vendor facet schema exposed by Cloud Router. */
export interface OfficialPricingProductVendorFacet {
  /** Code field on official pricing product vendor facet. */
  code: string;
  /** Count field on official pricing product vendor facet. */
  count: string;
  /** Id field on official pricing product vendor facet. */
  id: string;
}
