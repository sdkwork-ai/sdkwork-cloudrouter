/** Official pricing product region facet schema exposed by Cloud Router. */
export interface OfficialPricingProductRegionFacet {
  /** Code field on official pricing product region facet. */
  code: string;
  /** Count field on official pricing product region facet. */
  count: string;
  /** Id field on official pricing product region facet. */
  id: string;
}
