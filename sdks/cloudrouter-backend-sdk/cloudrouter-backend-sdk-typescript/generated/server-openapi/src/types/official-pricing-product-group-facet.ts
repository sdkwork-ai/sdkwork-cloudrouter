/** Official pricing product group facet schema exposed by Cloud Router. */
export interface OfficialPricingProductGroupFacet {
  /** Code field on official pricing product group facet. */
  code: string;
  /** Count field on official pricing product group facet. */
  count: string;
  /** Id field on official pricing product group facet. */
  id: string;
}
