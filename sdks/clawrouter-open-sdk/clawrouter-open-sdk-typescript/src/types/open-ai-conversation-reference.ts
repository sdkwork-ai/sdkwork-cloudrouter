import type { ProviderJsonValue } from './provider-json-value';

/** OpenAI-compatible open ai conversation reference schema exposed by Claw Router. */
export interface OpenAiConversationReference {
  /** Conversation identifier. */
  id?: string;
}
